#!/usr/bin/env python3
from pathlib import Path
import sys

sys.path.insert(0, str(Path(__file__).resolve().parent.parent))
from cual.session import main
try:
    main()
except (ValueError, OSError) as exc:
    sys.exit(f'Cual session: {exc}')
