#!/usr/bin/env python3
from pathlib import Path
import sys
sys.dont_write_bytecode = True

sys.path.insert(0, str(Path(__file__).resolve().parent.parent))
from lcu.session import main
try:
    main()
except (ValueError, OSError) as exc:
    sys.exit(f'LCU session: {exc}')
