# Installed-app development verification, 2026-09-23

This records the unpublished thin LCU build against the official ChatGPT Linux application 26.915.31945 and CUA 0.0.16/20260915001755-492f19756c31. It is not a release certificate.

## Earlier cleaned-source artifacts

| Artifact | SHA-256 | Bytes | Tar members | Execution |
| --- | --- | ---: | ---: | --- |
| `lcu-0.3.0-dev-linux-arm64.tar.gz` | `2a6c653e6cfdb1744dea67b33ce5d48b1436754e295ee029b1fa006b771680a6` | 1,648,741 | 864 | Native ARM64 Docker on Apple Silicon |
| `lcu-0.3.0-dev-linux-x64.tar.gz` | `74c36aa1b2799b5a0ac0a0ad8ed3b59fc2d0c24f40e50d0244fc3cc5a1606036` | 1,649,158 | 864 | Emulated x86-64 Docker on Apple Silicon |

The archives are under `/private/tmp/lcu-clean-final/v2-arm64/` and `/private/tmp/lcu-clean-final/v2-x64/` on this development host. Both sidecar hashes match. A member audit found no installed app, original instruction tree, generated reference tree, `.deb`, `ChatGPT` binary, or `app.asar`. The release contains the LCU launcher, installer, thin wrapper skill, lock, documentation and pinned agent registration tools. The checked-in copied instruction, reference and embedded-browser source was removed from the working tree.

Both exact archives passed `tests/offline.sh` with `--network none` and SHA-checked local official packages. The gate ran 50 Python tests, selected-user installation, byte-identical local instruction references, seven agent registrations in user and project scope, ownership, idempotency, preservation of unrelated configuration, destination-resolved portable export, a generic MCP client using that export as the target account, and the GTK/X11 desktop fixture with accessibility, input, file oracle, screenshot and timeout recovery.

The immediately preceding cleaned-source archives passed the full original-versus-LCU `tests/differential.sh` suite on both architectures with optional audio and real XFCE discovery. Results: 25 native cases and complete MCP schemas/docs matched, 12 original configuration modes matched, selected-home trust and an outside-root negative matched, scripted Codex model delivery and approvals matched, and Stop/Interrupt/SubagentStop lifecycle matched. Both architectures delivered nonzero stereo 24 kHz audio through MCP. Evidence is under `/private/tmp/lcu-clean-final/differential-arm64/` and `/private/tmp/lcu-clean-final/differential-x64/`. A tar-member digest comparison proves every `bin/`, `lcu/`, `scripts/` and `skills/` file in the final archives is byte-identical to those fully exercised archives. Only `README.md`, bundled documentation and the derived `bundle.json` changed.

## Chrome boundary

Disposable Ubuntu OrbStack VMs on both architectures launched sandboxed Google Chrome 154.0.8037.57 with official extension 1.26.901.11451 and the original native messaging host. The unmodified LCU path discovered Chrome but failed the first tab action with `Codex auth token is unavailable`; this is not action success.

A **disposable ARM64 test only** wrapped the original native host and omitted `agentRequestHeaderEnabled` from `getInfo` only when the extension itself returned `false`. A generic MCP client advertised elicitation and explicitly approved only `http://127.0.0.1:8080`. With no Codex sign-in, the original extension then navigated to the local page, exposed its accessibility tree, accepted Unicode typing and a button click, and returned a screenshot. The page showed the expected saved text. The test profile's original native-host manifest was restored afterward. This earlier experiment did not establish equivalence to every remote request-header policy.

Before explicit authorization for the local override, automatic approval review rejected the proposed persistent native-host adapter because omitting the policy field could weaken a browser security boundary. The earlier OrbStack VMs had no AppArmor; the separate AppArmor-enabled Ubuntu run is recorded below.

### What the request header does

The pinned original browser service fetches `https://chatgpt.com/backend-api/aura/identity` through its trusted fetch interface, requires a user ID, initializes Statsig for that user, and reads the `codex_browser_use_agent_request_header` feature gate. It fails if the gate is unavailable or unrecognized. The official extension reports `agentRequestHeaderEnabled` from its local state; on a fresh profile this is `false`, which makes the browser service read the gate on the first relevant session request. If the extension already reports `true`, the browser service keeps it enabled without another gate lookup on that path. The service sends `agent_request_header_enabled` to the extension with relevant session requests. Once enabled, the extension stores the decision in Chrome's local storage and adds `x-browser-agent: ChatGPT/<session ID>` to requests from agent-controlled tabs using Chrome's `declarativeNetRequest` rules. The label is visible to destination websites. It is not a bearer credential, ChatGPT login cookie, or signed proof of agent identity. Sources inspected: `resources/plugins/openai-bundled/plugins/chrome/scripts/browser-service.mjs` in the pinned app and `background.js` in official extension CRX 1.26.901.11451 (SHA-256 `688d5c0a8141c9bee394d3738d4a177b448713c2fa9c29b5af1815bfddae46e1`).

The code establishes the mechanism, not OpenAI's product rationale. Our inference is that the header labels agent traffic and the user-specific gate controls its rollout. [OpenAI's extension documentation](https://learn.chatgpt.com/docs/chrome-extension) describes feature rollout and workspace controls but does not explain this particular header. The earlier proposed adapter would have hidden the extension's `false` capability from the original browser service, preventing that service from obtaining and applying a possible `true` decision. It was not adopted.

A later disposable Ubuntu Chrome run placed a **pass-through-only** native messaging relay in the isolated fixture profile. It forwarded every message unchanged, reproduced the original authentication failure, and confirmed that the extension's `getInfo` reply carried `result.agentRequestHeaderEnabled: false`. An attempted test change to report `true` instead was rejected by automatic approval review before execution because the review did not find specific authorization for that mechanism. The user subsequently authorized that exact local override. Source inspection found no supported local setting that supplies the gate decision without identity: `BROWSER_USE_DISABLE_AMBIENT_NETWORK=1` skips identity initialization, but the request-header decision then fails with `Browser request-header policy requires caller identity.`

### Ubuntu AppArmor host test

The **exact ARM64 thin archive** above was installed for an unprivileged `lcutest` account at `/opt/lcu` in a separate UTM Ubuntu 24.04.5 ARM64 VM using the pinned official `.deb`. The VM had `lockdown,capability,landlock,yama,apparmor` active; `aa-status` reported 117 loaded profiles and 23 in enforce mode. We did not disable AppArmor or pass Chrome's `--no-sandbox` flag. This is a native ARM64 Ubuntu VM test, not the earlier OrbStack VM or a root-container result.

`tests/desktop.sh` passed against the installed original Node/REPL and a real X11/GTK desktop fixture: MCP discovery, Linux docs, AT-SPI, Unicode input, keys, paste, secondary action, window isolation, save oracle, X11 fallback coordinates, JPEG, error cases, persistence, reset, and timeout recovery. The original Chrome native messaging host, official extension 1.26.901.11451, and Google Chrome 154.0.8037.57 also passed `tests/browser_session.sh` discovery and matched the original-versus-LCU authentication boundary. Browser actions remained blocked by `Codex auth token is unavailable`; this test does not establish no-sign-in action success.

Observed process labels were `unconfined` for the original CUA Node, REPL, and Codex processes, and `chrome (unconfined)` for Chrome and the original extension host. [Ubuntu's 24.04 release notes](https://documentation.ubuntu.com/release-notes/24.04/) describe Chrome's `unconfined` profile as a compatibility path for user namespaces. The package's AppArmor profile names its Electron UI executable, which LCU did not run. The kernel audit did record `unprivileged_userns` denials for `bwrap` capabilities `setpcap` and `net_admin`; the native fixture and browser discovery still completed. Logs remain in the disposable VM at `/home/lcutest/native-desktop.log`, `/home/lcutest/browser-test-second.log`, and `/home/lcutest/process-labels.log`. That run closed the ARM64 native AppArmor test; the later relay test is below.

## Installed native-host relay on ARM64 AppArmor host

After explicit user authorization, LCU added a small relay between the official extension and its original native host. It changes only a native `result.type: extension` reply whose `agentRequestHeaderEnabled` value is `false` to `true`. This skips the original browser service's user-specific Codex gate lookup on that path and makes the official extension label agent-controlled tab requests. The original browser service, native host, extension, and MCP site approval still operate. The relay lives in LCU source and the thin archive; original plugin code is loaded from the selected official app and copied only into the target account's private writable host directory as required by the original installer.

The current ARM64 archive at `/home/lcutest/lcu-current-build/lcu-0.3.0-dev-linux-arm64.tar.gz` in the disposable UTM Ubuntu 24.04.5 VM had SHA-256 `cfa25a0a866eab963d32b3e119382bb56311ac85b8e16ca6910033eb567a0cc9`, 1,649,558 bytes and 867 tar members. It was installed offline from the pinned official `.deb` as an unprivileged account under `/home/lcutest/lcu-current-installed`. AppArmor remained enabled, and Chrome ran without `--no-sandbox`. `tests/browser_session.sh` installed the relay and loaded official Chrome 154.0.8037.57 with official extension 1.26.901.11451 in an isolated profile. A generic MCP client approved only `http://127.0.0.1:8080`; it navigated, typed `LCU hello Åß🙂`, clicked Save, observed the saved page, obtained a screenshot, and closed the tab without Codex sign-in. The local fixture received `/`, `/favicon.ico`, and `/save?...` with `x-browser-agent: ChatGPT/<exact LCU session ID>` on each request. Full test output is `/home/lcutest/lcu-current-browser.log` in the disposable VM.

Process sampling during a repeat browser action showed LCU's Python relay and the original extension host under `chrome (unconfined)`, while the selected Node, node_repl, and Codex executables were `unconfined`; Chrome itself was `chrome (unconfined)`. The action again passed with AppArmor active. The process sample is `/home/lcutest/process-labels-current.log`. This proves the installed ARM64 browser path and the website-visible header under the tested versions and local approval. It does not establish that a local header-on decision matches OpenAI's account-specific rollout policy, or verify native x86-64 host behavior. No release or push followed these checks.

## Current cross-architecture release gates

After the relay was added, `tests/run.sh` passed on native ARM64 Docker and emulated x86-64 Docker using the pinned official packages and `--network none` for installation. Each gate built a thin archive, installed it offline, passed 54 Python unit tests, registered seven agents with user and project scope, verified a portable generic MCP export, and passed the GTK/X11 desktop fixture. The ARM64 archive in `.verification/arm64.13GKWv/` has SHA-256 `890050224018536c6aa2068e100d1a1b94dc1ba4c7cfae15477597264f26686f`; the x86-64 archive in `.verification/amd64.YUKOzQ/` has SHA-256 `686adbf12d20e24981b3d361ce8ee7a5f99ba6397f36238a68f9ead1f59aaaea`. Each has 865 tar members, including `lcu/native_host.py`, and neither has the original app tree or copied instruction references. `git diff --check` passed.

An emulated x86-64 `tests/browser-run.sh ... chrome` attempt installed the release and native host, but Chrome stopped before DevTools startup: its sandbox could not create a namespace in that Docker environment (`Failed to move to new namespace: ... Operation not permitted`). The test did not use `--no-sandbox`. This was a Docker environment startup failure; the separate VM result below tested browser actions.

### x86-64 Ubuntu Chrome VM test

The exact final x86-64 thin archive (SHA-256 `91a9be6ef6f58943e85f9a0e3380b5b2e8bcd33a76dda97dead141b3570aa351`) was installed offline from the pinned official `.deb` in the existing disposable Ubuntu OrbStack VM `lcu-parity-x64-20260922`. The VM reported `uname -m: x86_64` and ran under Apple Silicon emulation. An unprivileged `lcu` account launched Google Chrome 154.0.8037.57 with its sandbox enabled and official extension 1.26.901.11451. `tests/browser_session.sh` passed discovery through the original native host and LCU's relay. A generic MCP client approved only the local fixture origin, then navigated, typed `LCU hello Åß🙂`, clicked Save, observed the saved page, captured a screenshot, and closed the tab without Codex sign-in. The local fixture observed `x-browser-agent: ChatGPT/<exact LCU session ID>` on `/`, `/favicon.ico`, and `/save?...`. The saved passing output is `/home/lcu/lcu-current-x64/browser-final.log` in that VM.

The first two VM attempts failed while Chrome wrote its profile because this disposable account hit its storage ceiling after the installer retained a 399 MB `.deb` cache. Removing only that test install's cached `.deb` and using a fresh profile resolved the environment failure; the complete browser test then passed. This proves the x86-64 Chrome path under emulation in this VM, not native x86-64 hardware or x86-64 AppArmor behavior. The separate ARM64 Ubuntu test above supplies the AppArmor evidence.

The current ARM64 and emulated x86-64 archives also passed `tests/differential.sh` with network disabled and real XFCE discovery: 25 native cases and complete MCP schemas/documentation matched the original app; 12 configuration modes matched; scripted Codex model delivery, approvals, and Stop/Interrupt/SubagentStop lifecycle passed. Evidence is under `/private/tmp/lcu-diff-current-arm64/` and `/private/tmp/lcu-diff-current-x64/`. The x86-64 result is emulated, not native hardware proof.

After updating the guides, final thin archives were rebuilt and placed in ignored `dist/`. The archives used by the installed VM tests had SHA-256 `2922c1ec14183c26ae4fe2b0b4d7f35b0f3b1a87d8351623b5018b8260821420` for ARM64 and `91a9be6ef6f58943e85f9a0e3380b5b2e8bcd33a76dda97dead141b3570aa351` for x86-64. After recording the x86-64 VM result in the bundled status page, the current `dist/` archives were rebuilt; their SHA-256 values are `479b53744cd88a50fecaca1139ac12d7e4c7769bb19e1b9e86b831f07abd9ff8` for ARM64 and `78cdca4ac58590e3a76ac39cafa951c20bc0df0d4a21929ed95f85ac29892345` for x86-64. Only `docs/PARITY-STATUS.md` and its derived `bundle.json` changed inside each archive. Both sidecars pass `shasum -a 256 -c`. Each archive has 865 members. A member-by-member digest comparison found their launchers, LCU runtime code, installer, wrapper skill, and agent tools identical to the corresponding archives exercised by the release and differential gates; bundled guides match the current source files. The final archives contain no original app tree, copied instruction references, or Python cache files. The builder now disables bytecode in its import check even when its caller does not set `PYTHONDONTWRITEBYTECODE`. Six older generated archives with copied upstream payloads were removed from `dist/` after the final archives passed verification.

After saving a tracked patch and untracked-file backup under `/private/tmp/lcu-history-cleanup-p3bvzeiy`, local `main` was reset to `origin/main` at `d1d9f96` with its working files preserved. The cleaned tree was committed as `c5f5613` directly on that base. `git merge-base --is-ancestor 95189d0 HEAD` returned false: the full-copy commit `95189d0` is not in the new main lineage. Draft PR #1 was closed, and `fix/full-linux-parity` was deleted remotely and locally after its clean disposable worktree was removed. Earlier published `main` and tagged history still exist. This was the history state before the later release work.

After adding installer failure tests, `tests/run.sh` passed on native ARM64 Docker and emulated x86-64 Docker with the pinned local official `.deb` and offline installation (`--network none`): 57 Python tests per architecture, agent registration, portable MCP export, and X11/GTK desktop actions. Evidence archives are `.verification/arm64.m6ev5h/lcu-0.3.0-dev-linux-arm64.tar.gz` (SHA-256 `cfc24ceead3f3c3b33834fc90d65a6cc020c6d957c96eb81597a5f85e63ee84f`) and `.verification/amd64.79V7ml/lcu-0.3.0-dev-linux-x64.tar.gz` (SHA-256 `5e6547d40c2a93cbd503876dc15d977c2beae3452d7de3da5776caa74e413c7d`). The new tests cover same-prefix concurrent installs, simulated apt dependency failure, and a conflicting `.next` symlink; they made no installer implementation change.

The x86-64 OrbStack VM reran the real Chrome session with the expanded `tests/browser_runtime.py`. The extension rejected a closed tab, site approval denial prevented the denied origin from being fetched, CUA reset reconnected, and a new MCP service process required fresh origin approval and sent the new session ID in the header. A disposable login form set a cookie and allowed navigation to its protected page. A slow navigation was requested once after the MCP test client timed out. The documented `cua.getTab({mention})` path rejected a stale tab title and claimed the tab when the snapshot matched. The browser and extension were real; the pages and credentials were local fixtures. The passing log is `/home/lcu/lcu-current-x64/browser-tab-mention.log` in that VM. This test does not establish behavior for remote account authentication, turn-scoped handoff cleanup, or independent agent products.

## Physical x86-64 host and Ubuntu AppArmor gate

A disposable Ubuntu 24.04.5 AMD64 guest ran with KVM acceleration and host CPU passthrough on a physical AMD Ryzen 7 2700X devbox. This was native x86-64 hardware execution in a VM, not the Apple Silicon emulation used above. An unprivileged `lcutest` account installed the then-current x86-64 thin archive (SHA-256 `78cdca4ac58590e3a76ac39cafa951c20bc0df0d4a21929ed95f85ac29892345`) at `/opt/lcu` offline from the pinned official package (SHA-256 `d27a9c02919cfe484dcc5f34584b9ea9fd0d7a65c69dcc872b5bdcfa0efb5983`). The official Ubuntu cloud-image SHA-256 was `612b2c0cc1bc413a6cb8c38fd611794caf0f2b436c50013d8b3794db12ad7354`. The guest had 117 AppArmor profiles loaded, 23 enforcing. The browser was Google Chrome 154.0.8037.57 (package SHA-256 `66c0645f6a19871bab2844b8537c11a0db2e7d3bea8ef85a1c7cb52a54e65a3e`) with official extension 1.26.901.11451 (CRX SHA-256 `688d5c0a8141c9bee394d3738d4a177b448713c2fa9c29b5af1815bfddae46e1`).

`cd /home/lcutest && dbus-run-session -- bash desktop.sh /opt/lcu/current/bin/lcu` passed the installed native desktop fixture. `bash /home/lcutest/guest-tests/run-browser.sh` passed the real Chrome session using `tests/browser_runtime.py` SHA-256 `ae1b8d2b39cbb5fca56905dda01bd87722f1dd7bb905898318ddeef5c51f5097`. It covered discovery through the official extension and native host, no-sign-in navigation/input/save/screenshot, local login cookie and protected page, denied-origin blocking, closed-tab rejection, CUA reset, fresh approval and header session after a new MCP service process, no replay after a client timeout, and the documented stale-title rejection/exact tab claim. The fixture pages and login were local; the browser and extension were original. The first desktop attempt failed because this runtime-only install had not generated the instruction references, then the account completed setup and the second run passed. The first failing log was retained.

Chrome ran without `--no-sandbox`. Renderer samples showed `NoNewPrivs:1` and `Seccomp:2`. Chrome, LCU's relay and the original extension host were labeled `chrome (unconfined)`; the selected Node/REPL were `unconfined`. AppArmor logged nonblocking `bwrap` denials for `setpcap` and `net_admin`; it was not disabled. Evidence remains on devbox under `/var/tmp/lcu-native-x64-20260923/`: `desktop-second.log` SHA-256 `df7b660fb9d45de66cce47cf37f74d9c71963f1fcb32cbb221cd4d83475ecf29`, `browser-session-final.log` `aa1cb095487e9efdb2d6eac04c34e70e8f780860b0d1ec107c61b0b0192df19e`, `process-labels-browser.log` `779f40810c11668a5673babd473de110cdeb32a406aed95b66daaf344fd10c51`, `apparmor-kernel-browser.log` `8985654f01c9a9bcb7a6009779dcd26eab28a83dba384ed039e0387427f8aa40`, and `apparmor-denials-only.log` `43489e84578196411249744a661e272bd00384e7fa23fa5d81ab6c65c6351c94`. `COMMAND-MANIFEST.md` (SHA-256 `b7e6a8372a7dfc2c009a9470ca55fa1631c6a012c24392a4686ea8a8be2efadd`) records full commands. The guest received a systemd poweroff; QEMU exited and its disposable container stopped. The disk and logs were preserved.

## Process-level installer failure and concurrency gates

After adding a two-process installer race and failure checks to `tests/offline.sh`, the full offline gate passed on native ARM64 Docker and emulated x86-64 Docker. The exact commands were `bash tests/run.sh linux/arm64 /private/tmp/silo-cua-engine.yy0cvD/chatgpt_arm64.deb` and `bash tests/run.sh linux/amd64 /private/tmp/lcu-x64-input/packages/chatgpt_amd64.deb`, both exit 0. Each ran 57 Python tests, seven agent registrations in user and project scopes, portable generic MCP export, and the X11/GTK desktop fixture. That earlier gate produced `.verification/arm64.c7G9lu/lcu-0.3.0-dev-linux-arm64.tar.gz` (SHA-256 `fd88b01e73ba7189fdf20e34bbf1d031aa23978da81970ac90f6517c8fc61c92`) and `.verification/amd64.4OZGaU/lcu-0.3.0-dev-linux-x64.tar.gz` (SHA-256 `8b3d372ae3bb0f27e9c8f809ad3870f52d4116898c3119f7069941b85db50580`). Both archive sidecars passed `shasum -a 256 -c`.

Inside each network-isolated test image, an intentionally corrupt package failed without changing `current`. Two independent installer processes then raced against one prefix and both exited successfully; the selected release matched its `bundle.json` manifest and no staging residue remained. A separate installer process encountered a test-local `apt-get update` stub returning exit 100; the stub's invocation was asserted, and `current` still selected the previous usable release. This checks the system-library failure boundary without modifying host package state. The pre-existing unit tests also cover an unexpected `.next` symlink, interrupted staging, package drift, and managed-generation validation. A real apt transaction remains outside this reversible selection guarantee.

## Independent agent-product browser checks

A disposable Linux ARM64 image ran the exact then-current thin archive (SHA-256 `479b53744cd88a50fecaca1139ac12d7e4c7769bb19e1b9e86b831f07abd9ff8`) with the pinned official app package (SHA-256 `b94c494b5f0fd7c720fa6fccd5ef609879affc62332ca930ed29b907d537bc6d`), a local scripted model endpoint, a loopback page fixture, and external networking disabled. The image was `lcu-independent-agent:arm64` (`sha256:1534dc65ac9e85fec256bb02c07aef9815fffcecf5c294cf2e1bfa9c37c01cfe`). These were real agent products and a real browser, but the model and page were disposable local fixtures.

OpenCode 1.18.32 received LCU's `lcu_js`, `lcu_js_add_node_module_dir`, `lcu_js_reset`, and `lcu_turn_ended` tools. Its local model received the full server guide and JS tool description. The MCP client advertised `{"roots":{}}` without elicitation. Its attempted Chrome navigation returned a tool error because the original browser provider could not complete site approval; the fixture received zero requests and saved zero values. A dedicated rerun preserved `/private/tmp/lcu-independent-agent/opencode-evidence/opencode-report.json` (SHA-256 `33e23aff1f1329a7aedbfbfee4aecfe78165a4cecabcb64842578f4ba547cb1c`), `transcript.txt` (`21de6fac0f9e053c4c553e0d6ee1d70abe5a5907a855446f90839ce54da950c4`), and the exact `COMMAND.txt` (`b20ebef05355373e373677f1d7c46b998cae2f301ab1465b8e6cc914220c7344`).

Goose 1.51.0 ARM64 (official release archive SHA-256 `e47da01a5378383b050bd736be3989d94ba8160d0a7e7df8280ddb2aec6fd306`, checked against its release asset digest) advertised elicitation in its attempted MCP initialization, but first sent `server/discover`. The pinned LCU MCP server expects `initialize` first and exited. A disposable proxy consumed that first message and replied with an empty result, after which Goose closed the connection before sending initialization. No LCU tool or guide reached Goose's model, no `elicitation/create` occurred, and the fixture received zero requests. The saved `/private/tmp/lcu-independent-agent/report.json` has SHA-256 `61fc63c07bc92e31744f930b2a37332365e5ba57ae3f2ce4e80f2745a62a855b`; its captured model request has SHA-256 `9b9cf014afd1ef391b02271766a432b0f6c70e91bae5bd3067f200d66aeaab27`. No repository workaround or approval bypass was added. A separate agent product has not yet completed LCU's live browser approval contract.

Further inspection of Goose's official client source identified the empty discovery reply as invalid for its fallback path. A disposable proxy instead returned the standard JSON-RPC `-32601 Method not found` error to `server/discover` and forwarded subsequent MCP messages unchanged. Goose then initialized with elicitation, received LCU tools and the full guide, invoked `lcu__js`, and received a real `elicitation/create` scoped to `http://127.0.0.1:8080`. Its headless `goose run` mode could not collect input and failed closed before any fixture request. The corrected proxy report `/private/tmp/lcu-independent-agent/goose-discovery-compat-evidence/goose-discovery-compat-report.json` has SHA-256 `4374cb732f0e332f21e082c9850514161906acefd97cb8d7f4ed014265988828`; its transcript has SHA-256 `9155123d15b0174e4f8e808ba0f0c767ca26b28c71168b65ae812b5fae4b7c9d`. A later PTY attempt with Goose's explicit `--interactive` flag stopped at its separate first-run anonymous telemetry opt-in before MCP initialized. No telemetry choice, site approval, or browser request was sent. Its final report SHA-256 is `ff2abaed58db42ce6b99e8469b87b7b57af1060451733e028f0b9cd55def2eee`. The disposable container exited. This identifies a small protocol-compatibility seam but does not prove an independent product completed an approved browser action.

The disposable PTY harness initially failed to recognize Goose's full telemetry prompt text. After correcting that matcher, it selected Goose's explicit No option; the test output confirmed telemetry disabled. Goose then showed `Allow Browser use to access http://127.0.0.1:8080?` and an `Approve? Yes / No` choice. The harness accepted only that exact localhost request. With the test proxy, the fixture saved `Goose fixture action Åß🙂` and saw `x-browser-agent: ChatGPT/<LCU session ID>` on its page, favicon, and save requests. That established the agent product's approval path, but still depended on a test-only proxy.

LCU then added the opt-in `--mcp-discovery-compat` flag. It consumes only an initial JSON-RPC `server/discover`, replies `-32601 Method not found` with the same ID, and execs the unchanged installed original MCP server with the remaining stdin/stdout. `tests/test_runtime.py` passed 14 tests, including no read-ahead into the next `initialize` message and refusal of another initial method. A disposable installed ARM64 candidate archive (SHA-256 `2d7aab6838241f472a7e2f894c4bd4a0ac7b247e460780ec2cfb9eb761d45425`) then repeated the Goose 1.51.0 interactive fixture **without the proxy**. The generated portable MCP export had the flag appended to its `args`. Goose received the full original guide and LCU tools, issued `lcu__js`, presented the exact localhost site request, and accepted it through its interactive UI. The original extension made `/`, `/favicon.ico`, and `/save?...` requests with `x-browser-agent: ChatGPT/lcu-4f9df184-e2db-48da-916a-3f4e65379d70`; the fixture decoded the saved value as exactly `Goose fixture action Åß🙂`. External networking was disabled. The interactive client remained open after the scripted result and was stopped by the bounded harness; that post-result exit was not an action failure. The direct-flag report at `/private/tmp/lcu-independent-agent/goose-direct-flag-evidence/goose-pty-compat-report.json` has SHA-256 `58caf225655074924518f2a5a1616e89594b94d7208dac6fa3dcb3d1c4c0f109`, and its captured model request has SHA-256 `ee93d34cc2a4885d54c217d6aba513ccf1b09d08d4aa35adebd5b8bec3522c26`. This closes the separate agent-product browser action gate for that pinned Goose mode; OpenCode's missing elicitation remains an agent-host limitation.

## Browser turn cleanup and extension reload

The physical x86-64 Ubuntu KVM guest above was restarted from its preserved disk. With AppArmor still active, a disposable localhost fixture and the installed LCU archive opened three separate tabs through the official Chrome 154.0.8037.57 and extension 1.26.901.11451. In one authentic LCU session/turn, the test left the first temporary tab unmarked, called `markDeliverable()` on the second, and `markHandoff()` on the third. It then invoked the installed `turn_ended` MCP hook with `hook_event_name: Stop` and the actual session/turn IDs from `nodeRepl.requestMeta`. After the hook, the unmarked tab was absent and both marked tabs were present. Site approvals targeted only `http://127.0.0.1:8080`.

The test then invoked `chrome.runtime.reload()` in the official extension service worker of that disposable profile. LCU rediscovered the extension and opened another approved fixture page. The first attempt met `ERR_CONNECTION_REFUSED` because the earlier fixture server had already closed; restarting that local server made the post-reload page action pass. No browser or AppArmor policy changed. Evidence remains on devbox under `/var/tmp/lcu-native-x64-20260923/`: `turn-cleanup-result.log` SHA-256 `f3ffc69ce413fb6e57c6d0185fd1aadf42b179cfceed6a266a010ad6741112d6`, `extension-reload-result.log` `ffe3633998a20ee47056e2fb2d03e11c2d7ab6573ec257fb110175d38a8a7a13`, `turn-cleanup-manifest.txt` `20a6b765722e897fe12453ef1b891d152b24f8a1e9e5da777543ff2be0df930a`, and the guest test script `4af5cf88555d6739f3c1f8633e2c94e3ddc58a673bbfc32b9a8df37787957c06`. The dedicated browser launcher and Xvfb were stopped; the guest was powered off through systemd, and its QEMU container stopped. The disk and evidence were preserved.

## Audited local thin archives

The versioned `0.3.0` full offline gates ran `bash tests/run.sh linux/arm64 /private/tmp/silo-cua-engine.yy0cvD/chatgpt_arm64.deb` on native Apple Silicon Docker and `bash tests/run.sh linux/amd64 /private/tmp/lcu-x64-input/packages/chatgpt_amd64.deb` under x86-64 emulation. Both exited 0. Each ran 59 Python tests, network-disabled package installation and failure checks, seven agent registrations, portable MCP export, and the X11/GTK desktop fixture. Their exact archives are `.verification/arm64.q7o1QK/lcu-0.3.0-linux-arm64.tar.gz` (SHA-256 `a3df18f3422b1490f89b142b10eb2244617710c996ffe93ce5bcd9fe4efd1b90`) and `.verification/amd64.guVVac/lcu-0.3.0-linux-x64.tar.gz` (SHA-256 `c9d623f7b5360230e6159da73495d038c29a62d2438d2eb16e5dfe8f7db4bfc3`). Both sidecars passed. Each tar has 865 members including its root.

Both versioned archives then ran the original-versus-LCU `tests/differential.sh` in the matching network-disabled image with real XFCE discovery, and both exited 0. Each matched 25 native cases, complete MCP schemas and delivered documentation, 12 configuration modes, scripted Codex model visibility and approval, and Stop/Interrupt/SubagentStop lifecycle. Evidence is under `/private/tmp/lcu-release-diff-arm64/` and `/private/tmp/lcu-release-diff-x64/`. These archive gates ran after the opt-in Goose compatibility adapter and version bump; the native Ubuntu Chrome and AppArmor tests above ran on earlier thin archives with the same default browser path.

The final ignored `dist/` assets were rebuilt from the frozen source in matching Linux images. The ARM64 archive SHA-256 is `a8bb483bb6c796da3f4d86721e36dcab79540466c708f8cef10e365f9214108b`; the x86-64 archive SHA-256 is `4595e1deb06a52ad8e06c891ff7f011f35ed8a5a7d703bf295d16a1f233cb179`. Both sidecars passed `shasum -a 256 -c`. A member-by-member comparison to each versioned gate archive found the same 865 names, types, modes and links; the only changed files were `docs/PARITY-STATUS.md` and its derived `bundle.json`. No OpenAI application, copied instruction tree, old embedded-browser host, `.deb`, or Python bytecode cache is present.
